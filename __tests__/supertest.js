const request = require('supertest');
const server = 'http://localhost:3000';

//testing route integration

//endpoints to test: /container, /container/exit, /container/stop, /container/start, /container/pause, contatiner/unpause,
// /metrics, /metrics/cpu

//setting conditions before or after each test
describe('Route integration', () => {

}
